// 'abcdef'.substring(3, 1); // => 'bc'
// 'abcdef'.slice(3, 1);     // => ''

// let obj = { fruit: 'apple', vegetable: 'carrot' };
// obj.fruit; // => 'apple'
// obj.fruit[1]; // => 'p'
// obj['vegetable']; // => 'carrot'
// obj['vegetable'][0]; // => 'c'

// let numbers = [1, 2, 3, 4];
// numbers[0] = numbers[0] + 1;  // => 2
// numbers[4] = numbers[4] + 1;
// numbers;    // [ 2, 2, 3, 4, NaN ]

// > 'abc'[5]
// undefined
// > 'abc'.charAt(5)
// ''
// /*  */

/* 
input: string
output: an array of strings (substrings)
rules
explict: return case sensitive palindromes, e.g. dad, not Dad
no palindromes returns an empty array
an empty string returns an empty array



implicit




*/